export default function Home() {
  // const getrand = (count) => {
  //   return Math.floor(Math.random() * count);
  // };
  // const num = getrand(2);
  // console.log(num);
  // if (num == 0) {
  //   throw new Error("num val is error -> ", { num });
  // }

  return (
    <>
      <h1>hahhaa</h1>{" "}
    </>
  );
}
